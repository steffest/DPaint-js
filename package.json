{
    "name": "DPaintJS",
    "version": "0.2.0",
    "description": "Webbased image editor with a focus on retro Amiga file formats in plain JavaScript",
    "repository": "https://github.com/steffest/dpaint-js",
    "author": "Steffest",
    "license": "MIT",
    "scripts": {
        "clean": "rm -rf dist && rm -rf .parcel-cache",
        "cleanwin": "del /q .\\dist\\*",
        "build": "vite build --base=./ --outDir dist_test",
        "deploy": "vite build --base=./",
        "start": "npx http-server",
        "test": "playwright test --config ./tests/playwright.config.js"
    },
    "source": "index.html",
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "playwright": "^1.58.2",
        "playwright-core": "^1.58.2",
        "vite": "^7.3.1"
    },
    "dependencies": {
        "mp4-muxer": "^5.2.2",
        "webm-muxer": "^5.1.4"
    }
}
